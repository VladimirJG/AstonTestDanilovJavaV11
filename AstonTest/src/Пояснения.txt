Задание №1 "EnteredNumber" 

"TestNum":
Логика проверки вынесена в отдельный метод, учтены вводимые значения <=7(отдельное условие "==7" не выводил), ввод дробных значений 
(не совсем понял необходимый результат на вводимые дробные значения, поэтому вывел как ошибку, однако если из алгоритма удалить первое условие "if",
дробные значения также будут учитываться при проверке.)

В интерактивном вводе написал три варианта алгоритма: 
"solution1" - try/catch; 
"solution2" - if(scanner.hasNextInt)(ввод дробного значения приводит к ошибке); 
"solution3" - if(scanner.hasNextInt)(дробное значение учитывается при проверке).

Дополнительно: использовал метод "Locale.setDefault(Locale.US);" с предварительным импортом "import java.util.Locale;" для корректного ввода дробного значения. 


Задание №2 "EnteredName"

Учтены все замечания: Ввод значения нерегистрозависим, "==" заменен на "equals" в неинтерактивном вводе, 
вопрос только по кодировке (Scanner sc = new Scanner(System.in,"CP866"), без нее алгоритм (интерактивный и .jar) отрабатывает нормально - "entNameInteractive", 
но если ее ввести алгоритм не считывает значение (переходит к "else"). Оба варианта приложены. "interactiveWithCoding.jar" удалил (не прикладываю), 
алгоритм не срабатывает -"interactiveWithCoding".

Задание №3 "Array"

Для перебора значений использовал "for/each". В интерактивном варианте для ввода значений использовал "for". Отдельно создал два файла Double(not/interactive). 
Написал возможность ввода массива чисел. Результат вычеслений остатка от деления, дробную часть не учитывает и в выводе всегда целое число.
Немного изменил условия добавив try/catch для контроля ввода элементов в массив (надеюсь это не запрещено).

Задание №4

По заданию замечаний не было, прикладываю уже проверенное.

Общие замечания: 

"_" из наименований классов и переменных убрал;
Структурированный проект подготовлен;
Сочетание клавиш "ctrl+alt+l" использовано, пробелы убраны;
Проект направлен полностью.
   

Приложенные .jar файлы: 
Задание№1: "solution1.jar","solution3.jar";
Задание№2: "intNameInteractive.jar";
Задание№3: "interactiveDouble.jar".
          
